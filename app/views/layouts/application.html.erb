<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>loopshare</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/"><img src="/logo_white.png" width="25" height="25" class="d-inline-block align-top" alt=""> loopshare</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        </ul>
        <ul class="nav navbar-nav flex-row justify-content-md-center justify-content-start flex-nowrap pull-right">
          <li class="nav-item dropdown active">
            <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fas fa-user"></i>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <% if user_signed_in? %>
                <a class="dropdown-item" href="/users/edit">Settings</a>
                <a class="dropdown-item" href="/users/sign_out">Sign out</a>
              <% else %>
                <a class="dropdown-item" href="/users/sign_in">Sign in</a>
                <a class="dropdown-item" href="/users/sign_up">Sign up</a>
              <% end %>
            </div>
          </li>
        </ul>
      </div>
    </nav>

    <% if notice %>
      <div class="alert alert-info" role="alert">
        <%= raw(notice) %>
      </div>
    <% end %>
    <% if alert %>
      <div class="alert alert-primary" role="alert">
        <%= raw(alert) %>
      </div>
    <% end %>
    <% if @notice %>
      <div class="alert alert-info" role="alert">
        <%= raw(@notice) %>
      </div>
    <% end %>
    <% if @alert %>
      <div class="alert alert-primary" role="alert">
        <%= raw(@alert) %>
      </div>
    <% end %>
    
    <%= yield %>
  </body>
</html>
